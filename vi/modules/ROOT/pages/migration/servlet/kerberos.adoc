= Di chuyển Kerberos

Đối với người dùng sử dụng tính năng hỗ trợ Kerberos của Spring Security, các dependency coordinates cho Maven và Gradle đã thay đổi kể từ khi tính năng hỗ trợ này được chuyển từ một module bên ngoài vào Spring Security.
Xem xref:servlet/authentication/kerberos/introduction.adoc[tài liệu Kerberos] để biết các dependency coordinates mới cho Maven và Gradle.
